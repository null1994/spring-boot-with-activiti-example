spring:
  profiles:
    active: dev

  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/web

  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: update
